{
  "name": "talvo",
  "version": "0.1.0",
  "private": true,
  "description": "Talvo - Sub0-powered talent intelligence and AI screening for recruiters.",
  "scripts": {
    "install:all": "npm install --prefix backend && npm ci --prefix frontend --ignore-scripts",
    "build": "npm run build --prefix backend && npm run build --prefix frontend",
    "build:backend": "npm run build --prefix backend",
    "build:frontend": "npm run build --prefix frontend",
    "dev:backend": "npm run build --prefix backend && npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend"
  },
  "dependencies": {
    "pnpm": "^10.33.2"
  }
}
