# Leave unset for mock mode (no backend required — best for judging demos).
# NEXT_PUBLIC_API_URL=

# Local Express/Mongo worker (mirrors Sub0 schemas for dev):
NEXT_PUBLIC_API_URL=http://localhost:5000

# Production: point to Sub0 API base URL once deployed.
# NEXT_PUBLIC_API_URL=https://api.your-sub0-project.lingoql.app
